{
  "name": "resizer",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/app.js",
  "scripts": {
    "resize": "node ./dist/app.js",
    "build": "tsc --rootDir ./src --outDir ./dist",
    "setup": "npm install & dtsm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yasupeke",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^0.30.0",
    "imagemagick": "^0.1.3",
    "progress": "^1.1.8"
  },
  "devDependencies": {
    "dtsm": "^1.1.0",
    "typescript": "^1.8.10"
  }
}

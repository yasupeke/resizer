/**
 * 並列処理最大数
 */
export const DEFAULT_PARALLEL_MAX_COUNT = 10;

/**
 * キャッシュファイル名
 */
export const DEFAULT_CACHE_FILE_NAME = '.cache';

/**
 * キャッシュファイルパス
 */
export const DEFAULT_CACHE_PATH = `${DEFAULT_CACHE_FILE_NAME}`;

/**
 * 設定ファイル名
 */
export const DEFAULT_CONFIG_FILE_NAME = `config.json`;

/**
 * 設定ファイルパス
 */
export const DEFAULT_CONFIG_PATH = `${DEFAULT_CONFIG_FILE_NAME}`;